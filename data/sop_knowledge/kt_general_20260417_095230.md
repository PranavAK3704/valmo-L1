# new rule for when manifest IDs are given and AWB level data is asked

by entering the manifest IDs in the valmo lost 2k24 query the corresponding AWBs also pop up in the result giving us the AWB level data for the manifest
manifest = bag
